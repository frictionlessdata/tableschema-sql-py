
{
    "primaryKey": "entry_id",
    "foreignKeys": [
        {
            "fields": "entry_id",
            "reference": {
                "resource": "articles",
                "fields": "id"
            }
        }
    ],
    "fields": [
        {
            "name": "entry_id",
            "type": "integer",
            "constraints": {
                "required": true
            }
        },
        {
            "name": "comment",
            "type": "string"
        }
    ]
}
