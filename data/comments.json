
{
    "primaryKey": "entry_id",
    "foreignKeys": [
        {
            "fields": "entry_id",
            "reference": {
                "fields": "id",
                "resource": "<bucket>",
                "bucket": "articles"
            }
        }
    ],
    "fields": [
        {
            "name": "entry_id",
            "type": "integer",
            "constraints": {
                "required": true
            }
        },
        {
            "name": "comment",
            "type": "string"
        }
    ]
}
